language: node_js
node_js:
  - "8"
before_install:
  - npm i -g npm@latest
  - sudo apt-get -qq update
  - sudo apt-get install -y wine
script:
  - npm run dist:wine
  - cat ./dist/heat.wxs
addons:
  artifacts:
    paths:
    - ./dist/*.msi
notifications:
  email: false